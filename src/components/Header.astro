---
import ThemeToggle from './ThemeToggle.svelte';
import NavMenu from './NavMenu.svelte';

interface Props {
	user: { id: string; email: string | undefined; displayName: string | undefined; avatar: string | null } | null;
}
const { user } = Astro.props;
---

<header class="site-header">
	<div class="site-header-inner">
		<a href={user ? '/' : '/login'} class="lockup" data-astro-prefetch>
			<span class="lockup-title">P2</span>
			<span class="lockup-subtitle muted">Engineering Workspace</span>
		</a>

		<div class="header-right">
			<ThemeToggle client:idle />
			{user && <NavMenu client:idle avatar={user.avatar} displayName={user.displayName ?? user.email ?? ''} />}
		</div>
	</div>
</header>

<style>
	.site-header {
		width: 100%;
		padding: var(--space-2) var(--space-2) 0;
		background: var(--color-frame);
	}

	.site-header-inner {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: var(--space-6);
		padding: var(--space-3) var(--space-6);
		max-width: var(--max-width);
		width: 100%;
		margin: 0 auto;
		background: var(--color-surface-raised);
		border-radius: var(--radius-md);
	}

	.lockup {
		display: flex;
		flex-direction: column;
		line-height: 1.3;
		border-bottom: none;
		flex-shrink: 0;
	}

	.lockup:hover {
		border-bottom: none;
	}

	.lockup-title {
		font-weight: 700;
		font-size: 1.1rem;
	}

	.lockup-subtitle {
		font-size: 0.8rem;
	}

	.header-right {
		display: flex;
		align-items: center;
		gap: var(--space-4);
		flex-shrink: 0;
	}

	@media (min-width: 769px) {
		.site-header {
			padding: var(--space-4) var(--space-4) 0;
		}
	}

	@media (max-width: 768px) {
		.site-header-inner {
			padding: var(--space-2) var(--space-4);
		}
	}
</style>
